local id = make_cell() -- make an element id
grid_cell(x, y) -- put the cell in the keyboard navigation grid


place() -- establish element size can only be done once

rectangle()

push_mask() -- left half
is_hovering() -- only true if hovering left half

change_mask() -- right half
is_hovering() -- only true if hovering right half

pop_mask()

draw_queue.draw()
{

}



keyboard_navigation.evaluate()
